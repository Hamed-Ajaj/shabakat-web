export interface BoxRecord {
  id: string;
  name: string;
  areaId: string;
  areaName: string;
  locationNote: string | null;
  notes: string | null;
  customerCount: number;
  createdAt: string;
}

